# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# R
.Rproj.user/
.Rhistory
.RData
.Ruserdata

# Python (hook tooling is stdlib; ignore stray caches)
__pycache__/
*.pyc

# Local scratch
tmp/
*.log

# large raw source material (Gemini .mhtml exports + prior drafts) — distilled into the
# template factory's docs/research + docs/health; kept local only, never committed
/temporal-expansion-ideas/

# meta-learner transient (durable learning/*.md + learning/sessions/ ARE committed)
/.claude/state/
/learning/queue/
